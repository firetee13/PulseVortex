# Development dependencies (for backward compatibility)
# Note: Use 'pip install -e .[dev]' for full development setup

pytest>=7.0.0
pytest-cov>=4.0.0
coverage>=6.0.0
black>=22.0.0
isort>=5.10.0
flake8>=4.0.0
mypy>=0.950

# Optional: for documentation
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0

# Optional: for performance profiling
memory-profiler>=0.60.0
line-profiler>=4.0.0
